version: '3.8'

services:
  reverb-bot:
    container_name: reverb-bot
    build: .
    restart: unless-stopped
    volumes:
      - ./bot_data.json:/app/bot_data.json  # Persist bot data
    environment:
      - DISCORD_TOKEN=youMTMzNjczODI1NTIxNDczOTQ2Nw.Gy5cmi.HJBvPBDuFxhuF99vanJvKhKy7DjgiQy1bmtlOs  # Store in Unraid instead of hardcoding